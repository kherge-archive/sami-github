{% macro class_item(class, short = false, indent = "") -%}
{{ indent }}- [`{{ short ? class.shortname : class }}`]({{ _self.markdown_path(class_path(class)) }}){% if class.shortdesc %} &mdash; {{ class.shortdesc }}{% endif %}
{%- endmacro %}

{% macro markdown_path(path) %}
{% spaceless %}
{{ path|replace({".html": ".md"}) }}
{% endspaceless %}
{% endmacro %}
